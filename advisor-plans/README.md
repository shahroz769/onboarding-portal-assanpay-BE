# Backend Improvement Implementation Plans

Generated by the `improve` skill on 2026-07-11 from backend commit `daf574b` and frontend commit `b1bb7a0`. The existing `Plan/` directory is an unrelated historical product plan, so advisor output lives in `advisor-plans/`.

Executors must read the selected plan completely, honor scope and STOP conditions, run every verification gate, and update the status below. These plans do not authorize pushing, merging, production deployment, or use of non-disposable databases.

## Execution order and status

| Plan | Finding | Title | Priority | Effort | Depends on | Status |
|---|---:|---|---|---|---|---|
| 001 | 4 | Restore a passing backend verification baseline | P1 | M | — | TODO |
| 002 | 11 | Establish integration coverage for critical invariants | P1 | L | 001 | TODO |
| 003 | 1 | Protect manual case creation | P1 | S | 001, 002 | TODO |
| 004 | 2 | Enforce queue authorization on comments/history | P1 | S | 002 | TODO |
| 005 | 3 | Remove public notification test route | P1 | S | 002 | TODO |
| 006 | 5 | Harden public request boundaries | P1 | M | 002 | TODO |
| 007 | 6 | Consume password tokens atomically | P1 | S | 002 | TODO |
| 008 | 7 | Make resubmission storage concurrency-safe | P1 | M | 002 | TODO |
| 009 | 8 | Serialize MID Go-Live activation | P1 | M | 002 | TODO |
| 010 | 9 | Hash public bearer tokens at rest | P2 | M | 002, 008, 009 | TODO |
| 011 | 10 | Reconcile agreement Drive files | P2 | M | 002 | TODO |
| 012 | 12 | Gate production deployment | P2 | M | 001, 002 | TODO |
| 013 | 14 | Split the case service by workflow | P3 | L | 001–011 | TODO |
| 014 | 15 | Reconcile backend/frontend documentation | P3 | M | 001–012 | TODO |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED — reason`, `REJECTED — rationale`.

## Dependency notes

- 001 must land first because the current compiler baseline is red and later changes cannot be verified reliably.
- 002 supplies the disposable-database harness and characterization coverage used by every security/concurrency plan.
- 010 follows 008 and 009 because token storage migration must not obscure unresolved token-claim races.
- 013 is deliberately last among code changes: behavior must be protected before the 7,457-line service is split.
- 014 follows contract/deployment changes so it records final behavior instead of becoming stale immediately.
- Finding 13, comment/history pagination, was explicitly excluded by the operator and has no plan.

## Cross-repository impact

Plans 001–012 and 013 are backend-first and preserve current frontend API shapes. Plan 006 permits a narrow frontend error-message change only if existing `413`/`429` handling is inadequate. Plan 014 intentionally updates documentation in both repositories. No frontend build should be run; its `AGENTS.md` allows tests/lint/format but says not to run builds unless specified.

## Findings considered and rejected

- **Finding 13 — paginate comments/history**: excluded by explicit operator direction; do not implement as a side effect of Plan 004 or 013.
- **Migration journal mismatch**: rejected after direct comparison showed unjournaled historical SQL was duplicated or superseded by journaled migrations, including the later Physical Agreement reconciliation.
- **Upgrade core dependencies**: not worth a plan now; `bun audit --production` reported no vulnerabilities and `bun outdated` showed only an `@types/bun` patch.
- **Replace custom JWT or Google Drive solely on architectural preference**: rejected because both are documented product choices; plans address concrete implementation risks instead.
- **Restrict all merchant endpoints by queue**: not planned because current route comments explicitly allow all authenticated roles and product policy is ambiguous. Re-audit only after an explicit merchant-visibility decision.

## Execution limitation

The `improve` skill requires source changes to be made by a separate executor in an isolated git worktree and reviewed before any merge. The current collaboration runtime shares one working directory between agents and exposes no isolated-worktree dispatch, so implementation was not dispatched from this session. A compatible executor can run these plans manually or through an environment supporting isolated agent worktrees.

